# RawFox JS

无依赖性、无侵入性的前端开发框架

```js
App(
	Text("Hello World"),
  	Flex(
  		H1("This is the head"),
  		Button({
  			onClick(){
  				const xhr = new XMLHttpRequest()
  			}
    	})
  	)
)
```

类似 `Flutter`原生开发体验。
